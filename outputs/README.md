# Outputs Directory

```
outputs/
├── foia_vendor_risk_dataset.csv   # Harmonised exclusions + procurement dataset
├── foia_vendor_risk_codebook.csv  # Column definitions and descriptions
└── provenance_notes.txt           # Source citations and processing rundown
```

Run `py -3 -X utf8 build_dataset.py` to regenerate these artefacts before packaging for Zenodo.
